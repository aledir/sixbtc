# ==============================================================================
# SIXBTC ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and fill in your actual credentials
# .env is in .gitignore and will NOT be committed to git

# ==============================================================================
# DATABASE (PostgreSQL)
# ==============================================================================
DB_HOST=localhost  # Use 'postgres' if running in Docker
DB_PORT=5432
DB_NAME=sixbtc
DB_USER=sixbtc
DB_PASSWORD=your_secure_password_here

# ==============================================================================
# HYPERLIQUID EXCHANGE
# ==============================================================================
# IMPORTANT: Keep these secret! Never commit to git!

# ------------------------------------------------------------------------------
# MASTER WALLET
# ------------------------------------------------------------------------------
# This is the main wallet that owns the subaccounts.
# Used by:
# - scripts/setup_hyperliquid.py to create subaccounts and agent wallets
# - FundManager for automatic topup transfers
# - AgentManager for credential renewal
HL_MASTER_ADDRESS=0x...  # Master wallet address
HL_MASTER_PRIVATE_KEY=0x...  # Master wallet private key (64 hex chars after 0x)

# ------------------------------------------------------------------------------
# SUBACCOUNT CREDENTIALS (AUTOMATIC)
# ------------------------------------------------------------------------------
# Subaccount agent wallet credentials are managed AUTOMATICALLY:
# - Created by: python scripts/setup_hyperliquid.py
# - Stored in: database (credentials table)
# - Renewed by: scheduler (daily check, renews 30 days before expiry)
#
# NO MANUAL CONFIGURATION NEEDED for subaccount credentials!
# Just configure HL_MASTER_ADDRESS and HL_MASTER_PRIVATE_KEY above,
# then run the setup script.

# ==============================================================================
# AI PROVIDERS (Optional - for strategy generation)
# ==============================================================================
# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI/Codex
OPENAI_API_KEY=sk-...

# Google Gemini
GOOGLE_API_KEY=...

# ==============================================================================
# ALERTING (Optional)
# ==============================================================================
# Email alerts (Resend, SendGrid, or SMTP)
EMAIL_FROM=alerts@sixbtc.com
EMAIL_TO=your@email.com
EMAIL_API_KEY=...

# ==============================================================================
# PATTERN DISCOVERY API (Optional)
# ==============================================================================
PATTERN_API_URL=http://localhost:8001
PATTERN_API_KEY=  # If authentication is required

# ==============================================================================
# MONITORING (Optional)
# ==============================================================================
# Sentry for error tracking
SENTRY_DSN=

# Prometheus for metrics
PROMETHEUS_PORT=9090
