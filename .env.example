# ==============================================================================
# SIXBTC ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and fill in your actual credentials
# .env is in .gitignore and will NOT be committed to git

# ==============================================================================
# DATABASE (PostgreSQL)
# ==============================================================================
DB_HOST=localhost  # Use 'postgres' if running in Docker
DB_PORT=5432
DB_NAME=sixbtc
DB_USER=sixbtc
DB_PASSWORD=your_secure_password_here

# ==============================================================================
# HYPERLIQUID EXCHANGE
# ==============================================================================
# IMPORTANT: Keep these secret! Never commit to git!

# Master address (for read-only queries - REQUIRED)
HL_USER_ADDRESS=0x...  # Your master wallet address

# ------------------------------------------------------------------------------
# SUBACCOUNTS (for trading)
# Each subaccount needs:
# 1. HYPERLIQUID_SUBACCOUNT_ADDRESS_N = the subaccount wallet address
# 2. HYPERLIQUID_PRIVATE_KEY_N = API wallet private key (generated from Hyperliquid UI)
#
# Generate API wallets at: https://app.hyperliquid.xyz/API
# One API wallet per subaccount (recommended for nonce isolation)
# ------------------------------------------------------------------------------

# Subaccount 1
HYPERLIQUID_SUBACCOUNT_ADDRESS_1=0x...
HYPERLIQUID_PRIVATE_KEY_1=0x...

# Subaccount 2
HYPERLIQUID_SUBACCOUNT_ADDRESS_2=0x...
HYPERLIQUID_PRIVATE_KEY_2=0x...

# Subaccount 3
HYPERLIQUID_SUBACCOUNT_ADDRESS_3=0x...
HYPERLIQUID_PRIVATE_KEY_3=0x...

# Add more subaccounts as needed (up to 100 supported)
# HYPERLIQUID_SUBACCOUNT_ADDRESS_4=0x...
# HYPERLIQUID_PRIVATE_KEY_4=0x...

# ==============================================================================
# AI PROVIDERS (Optional - for strategy generation)
# ==============================================================================
# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI/Codex
OPENAI_API_KEY=sk-...

# Google Gemini
GOOGLE_API_KEY=...

# ==============================================================================
# ALERTING (Optional)
# ==============================================================================
# Email alerts (Resend, SendGrid, or SMTP)
EMAIL_FROM=alerts@sixbtc.com
EMAIL_TO=your@email.com
EMAIL_API_KEY=...

# ==============================================================================
# PATTERN DISCOVERY API (Optional)
# ==============================================================================
PATTERN_API_URL=http://localhost:8001
PATTERN_API_KEY=  # If authentication is required

# ==============================================================================
# MONITORING (Optional)
# ==============================================================================
# Sentry for error tracking
SENTRY_DSN=

# Prometheus for metrics
PROMETHEUS_PORT=9090
